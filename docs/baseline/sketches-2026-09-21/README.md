# Closed program-sketch checkpoint

Both complete runs passed **839/839 required checks across 14 families** at clean implementation revision `c69f4847328825aebe70bf8f564fa97158ae450e`, using `leanprover/lean4:v4.34.0`. The configured denominator and observed complete scores agree. Each run includes the aggregate library/native-test/executable build. Build jobs may be cached and do not count independently rerun tests; the native tests add no external acceptance cases. The frontend and sketch runners never build. Synthesis budgets are cooperative and distinct from process timeouts.

| Family | 10,000 ms budget | 5,000 ms budget |
| --- | ---: | ---: |
| baseline | 278/278 | 278/278 |
| recursive | 9/9 | 9/9 |
| church | 28/28 | 28/28 |
| context | 95/95 | 95/95 |
| corpus | 350/350 | 350/350 |
| session | 6/6 | 6/6 |
| results | 10/10 | 10/10 |
| extended | 29/29 | 29/29 |
| recursion-gates | 5/5 | 5/5 |
| local-proofs | 6/6 | 6/6 |
| guard-gates | 5/5 | 5/5 |
| tree-composition | 3/3 | 3/3 |
| frontends | 8/8 | 8/8 |
| sketches | 7/7 | 7/7 |
| **Total** | **839/839** | **839/839** |

The [sketch fixtures](../../../tests/sketches/cases.json) exercise two owned holes in a disclosed `List.foldr` skeleton, one local-context hole in a polymorphic identity, complete correct and wrong bodies, duplicate labels, unfinished anonymous elaborator holes, and literal `False`. The fold retains all four original finite equations plus independent held-out proof/execution observations. The identity retains its universal original equation. These are guided synthesis and verification checks; they do not claim invention of a fold traversal or a universal fold theorem from finite examples. See the [sketch guide](../../sketches.md).

Every public sketch query runs with curated providers and native rules while checking an empty session provider inventory. Reference terms are withheld from the synthesis environment. Provider-restricted search, ownership/state restoration, profile checks, alternative acceptance, and cancellation are separate [native preparation](../../../tests/Leant2Tests/SketchPreparation.lean), [controller](../../../tests/Leant2Tests/Sketch.lean), [command](../../../tests/Leant2Tests/SketchCommand.lean), and [alternative](../../../tests/Leant2Tests/AcceptanceAlternatives.lean) tests in the build graph. Empty session inventory does not mean an empty provider array and does not establish a particular internal search route.

Each positive sketch case extracts its actual `leant2 sketch source:` diagnostic and replays the term in a fresh file importing **Lean only**, repeating the original contract, held-out proof, and executable checks. Only embedding indentation changes. The producer's aliases and compiler-adapter environment are absent. The certified native term remains authoritative; successful independent replay establishes displayed-source validity for these cases. The separate Lean-only reference process checks the known witness, wrong step, and wrong seed; its failure fails the suite even if the public score is 7/7.

The wrong complete body must record at least one rejected program, rather than mere exhaustion or a claim of global impossibility. Literal False requires the reported certified contract-negative category; native tests separately replay the actual certificate. All fresh negative cases verify absence of both bare `it` and numbered `it1`; malformed inputs require exactly the preparation error and exit 1. Native command tests separately cover preservation of prior results, and well-formed negative alias behavior. The public sketch command uses the project-relative session profile. These fresh fixtures have no additional session axioms, so its allowance matches the standard profile here. The separate [library API](../../library-api.md) retains its frozen closed-expression boundary, explicit query profiles, independently checked output, and caller-state restoration; this command does not turn unfinished sketches into valid `synthesize` inputs.

The existing [expected-type frontend fixtures](../../../tests/benchmarks/frontend-fixtures/cases.json) retain local data/proof, delayed shared type, local let/instance, indexed Vec, and negative gates. Three tactic cases replay actual emitted `Try this:` text after removing only the documented `[apply]` widget label and framing indentation. Those Lean-only replay processes remain separate from the three new sketch replays. All **29 original E8 cases remain required**: 25 positive cases and 4 impossible controls, with zero open cases. Tree, guard, recursion, and local-proof families retain separate obligations; ordinary public tree providers include `List.append`. Overlapping acceptance checks are not a count of distinct synthesis problems.


At **10,000 ms**, the full build log records **83 successful build jobs**. The raw ZIP has **202 files** (22 `.in`, 10 `.json`, 24 `.jsonl`, 24 `.lean`, 15 `.log`, 52 `.out`, 46 `.stderr`, 9 `.txt`) and **46 empty stderr streams**. The legacy audit contains **837 synthesis-query records**: 278 baseline queries, 511 queries across 22 retained REPL sessions, and 48 manifest queries. Separately, **8 frontend cases require 13 fresh Lean processes**, comprising 8 original stages, 3 independent suggestion replays, and 2 deliberate failure stages with 2 expected errors and 0 recovery warnings. The non-replay frontend sources contain 12 syntax occurrences; this does not count elaborator retries. The **7 sketch cases require 10 public stages**: 7 original commands and 3 completed-source replays, plus **1 separate uncounted reference process**. All 11 sketch/reference processes are retained in 37 files; 2 preparation errors are expected and 0 warnings occurred. The two malformed commands are included in the command count, not labeled successful searches. Church stretches report **0/13 solved**, with 13 bounded misses (13 `refuted`), outside the required denominator. Baseline diagnostics comprise 2 scored preflight errors and 1 unscored post-query error.

At **5,000 ms**, the full build log records **83 successful build jobs**. The raw ZIP has **202 files** (22 `.in`, 10 `.json`, 24 `.jsonl`, 24 `.lean`, 15 `.log`, 52 `.out`, 46 `.stderr`, 9 `.txt`) and **46 empty stderr streams**. The legacy audit contains **837 synthesis-query records**: 278 baseline queries, 511 queries across 22 retained REPL sessions, and 48 manifest queries. Separately, **8 frontend cases require 13 fresh Lean processes**, comprising 8 original stages, 3 independent suggestion replays, and 2 deliberate failure stages with 2 expected errors and 0 recovery warnings. The non-replay frontend sources contain 12 syntax occurrences; this does not count elaborator retries. The **7 sketch cases require 10 public stages**: 7 original commands and 3 completed-source replays, plus **1 separate uncounted reference process**. All 11 sketch/reference processes are retained in 37 files; 2 preparation errors are expected and 0 warnings occurred. The two malformed commands are included in the command count, not labeled successful searches. Church stretches report **0/13 solved**, with 13 bounded misses (13 `refuted`), outside the required denominator. Baseline diagnostics comprise 2 scored preflight errors and 1 unscored post-query error.

The baseline preflight diagnostics concern ill-typed/unknown contract expressions; its post-query diagnostic is the known Option-call type error outside scored query boundaries. The auditor reconstructs secondary transcripts from captured inputs, rescores actual outcomes, and verifies session provider history. Expected rejected declarations in session/results fixtures remain permitted only in their specified scope. Church setup has no unexpected comment/setup diagnostics. This archive is not diagnostic-free. Bounded Church misses do not prove mathematical impossibility; neither these sketch cases nor their source replays establish arbitrary dependent/recursive sketch support, carrier invention, or complete search.

The [manifest](manifest.json) binds the [pre-run snapshot](input-snapshot.json) to identical [post-run inputs](post-run-inputs.json): **173 source/configuration hashes**, **57 compiled-artifact hashes** for the complete recorded inventory, and **350 external fixture hashes**. Required modules are derived from the recorded runtime/test sources. External revisions and dirty flags are independent metadata. `leant2.exe` SHA-256 is `b0be7689ac75132d172bce63d88e6d77efbce5149573ecbe6c7aeaedadbace06`; native Lean SHA-256 is `a8040e2cab341c12116ab591fed9f761816f5f6b08554f6cc1680e86dfbba0a2`. Stable fingerprints and successful build logs are separate evidence.

The [10-second summary](10000-summary.json), [5-second summary](5000-summary.json), [10-second sketch receipt](10000-sketches.json), [5-second sketch receipt](5000-sketches.json), frontend and other detailed receipts, logs, and complete [10-second ZIP](10000-raw.zip) / [5-second ZIP](5000-raw.zip) preserve original bytes. ZIP member inventories/hashes/CRCs, copied receipts, source reconstruction, actual suggestions/completed terms, diagnostics, exits, and stage counts are independently rechecked. The archive preserves its [frozen auditor](archive-script.py.txt) and pinned [API](api-archive-script.py.txt), [frontend](frontend-archive-script.py.txt), [tree](tree-archive-script.py.txt), [base](base-archive-script.py.txt), and [utility](base-finish-script.py.txt) sources. Archive-local `.gitattributes` protects evidence bytes; this README is ordinary Git text outside the manifest hash scope.

The seven prior [p1-2026-09-21](../p1-2026-09-21/README.md), [induction-2026-09-21](../induction-2026-09-21/README.md), [local-proofs-2026-09-21](../local-proofs-2026-09-21/README.md), [guards-2026-09-21](../guards-2026-09-21/README.md), [tree-composition-2026-09-21](../tree-composition-2026-09-21/README.md), [frontends-2026-09-21](../frontends-2026-09-21/README.md), [library-api-2026-09-21](../library-api-2026-09-21/README.md) archives retain their captured Git trees and every working-tree file byte, including their narratives. Their historical scores and scopes are not extended retroactively. This is a validation receipt bundle with input fingerprints, not a standalone toolchain/executable replay bundle. No separate Python-test or focused development-pass count is inferred from these complete-run receipts.
